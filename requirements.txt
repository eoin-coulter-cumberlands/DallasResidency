# E-Commerce Business Analytics - Python dependencies
# Install with:  pip install -r requirements.txt

# --- Data engineering (data loading, cleaning, feature engineering) ---
pandas>=2.2
numpy>=1.26
openpyxl>=3.1        # read the raw .xlsx
pyarrow>=15.0        # parquet read/write for processed datasets

# --- Analysis & visualization (used by the analysts / dashboard) ---
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20

# --- Notebook / dashboard (optional, pick what your team uses) ---
jupyter>=1.0

# --- Final artifact generation ---
python-docx>=1.2
python-pptx>=1.0
